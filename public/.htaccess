RewriteEngine On
RewriteBase /mini_mvc/public/

# Rediriger toutes les requêtes vers index.php (sauf fichiers existants)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]

